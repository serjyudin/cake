	// Custom JS
$(document).ready(function(){
  $('.single-slide').slick({
    dots: true,
  });
  $('.multiple-slide').slick({
    infinite: true,
    slidesToShow: 2,
    slidesToScroll: 1,
    // dots: true,
  });
});
